# WARNING: Do NOT commit real secrets. Replace the placeholder value below
# before deploying, or use a secrets manager (Vault, Sealed Secrets, etc.)
#
# To create from the CLI:
#   kubectl create secret generic helep-secrets \
#     --namespace helep \
#     --from-literal=JWT_SECRET=<your-strong-secret>
apiVersion: v1
kind: Secret
metadata:
  name: helep-secrets
  namespace: helep
type: Opaque
data:
  # base64-encoded placeholder — REPLACE before production
  JWT_SECRET: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
